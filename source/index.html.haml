- @title = "Positive Externality: The Curious Task of Economics..."

- page_articles.each_with_index do |article, i|
  %article.post
    .meta
      .date= article.date.strftime('%b %e, %Y')
      .tags= article.tags.join(", ")
    %h1.title= link_to article.title, article
    .entry-content
      = article.summary
      = link_to "Continue →", article, :class => "more-link"

- if paginate
  %nav#pagenavi
    - if next_page
      = link_to "Next", next_page, :class => "next"
    - if prev_page
      = link_to "Previous", previous_page, :class => "prev"
      
